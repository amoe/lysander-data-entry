import {EventSequence, FlightEvent} from './event-sequence';
import {PartialDate} from './partial-date';

const E1D = 'financial crisis';
const E2D = 'icelandic volcano';

it('works', () => {
    // const seq = new EventSequence();
    // const e1 = new FlightEvent('financial crisis', new PartialDate(2008));
    // seq.addEvent(e1);
    // expect(seq.size()).toBe(1);
});


it('can be mapped', () => {
    // const seq = new EventSequence();
    // 
    // const e1 = new FlightEvent(E1D, new PartialDate(2008));
    // const e2 = new FlightEvent(E2D, new PartialDate(2010));
    // seq.addEvent(e1);
    // seq.addEvent(e2);
    // 
    // expect(seq.map(x => x.getDescription())).toEqual([E1D, E2D]);

});


